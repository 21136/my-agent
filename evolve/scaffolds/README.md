# 项目配方索引（Phase 43）

> 真源设计：[docs/PROJECT-RECIPES.md](../../docs/PROJECT-RECIPES.md)  
> **状态**：目录占位 · 配方 **待实现**（T-4303 起）

LLM / 人类选配方前可读本文；细则见各子目录 `manifest.yaml` 与同目录 `README.md`。

| recipe | 栈 | 状态 |
|--------|-----|------|
| `spring-vue` | Spring Boot + Vue 3 + Maven + pnpm | **done**（T-4303） |
| `fastapi-vue` | FastAPI + Vue 3 + venv + pip | **done**（T-4305） |
| `next-prisma` | Next.js + Prisma + SQLite | **defer** |

调用：`scaffold_project` · `recipe=<id>` · `phase=init|deploy`（工具待 T-4302）。

**勿与** `scaffold_demo`（`write_evolve` 历史演示名）混淆。
