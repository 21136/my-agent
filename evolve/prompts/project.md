<!-- prompt_id: project · version: 2.0.0 · phase: 47 -->

# 项目模式入口

本段在会话绑定项目（`active_shell=project` / 有 `project_root`）时注入 system（PROJECT-MODE §7b）；与 `core.txt`、**coding** 主题叠加。  
产品形态：**项目窗口** = 普通窗口中的项目聊天 + 左侧任务流侧栏。

**注入组成**：`project-boundaries.md` + `project-delivery-{solo|ritual}.md`（由 `project_delivery_profile` 决定，见 overlay）。
