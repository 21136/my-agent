# `pip_install` (archived)

`status = archived` · `main.py` removed (T-4310).

Use **`run_command`** (`python -m pip install …`). Package validation: `agent-core/pip_install_policy.py`.

Reference: [`docs/ARCHIVED-TOOLS.md`](../../../../docs/ARCHIVED-TOOLS.md).
