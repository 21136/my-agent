# `host_read` (archived)

`status = archived` · `main.py` removed (T-4310).

Use builtin **`read_file`** with `host:` paths. Implementation: `agent-core/host_tools.py`.

Reference: [`docs/ARCHIVED-TOOLS.md`](../../../../docs/ARCHIVED-TOOLS.md).
